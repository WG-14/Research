You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Do not treat the current diagnosis as proven fact. Start from the EC2 evidence below as the incident context, then verify or falsify each hypothesis by inspecting and testing the repository.

Incident context:

* EC2 live dry-run environment: `MODE=live`, `LIVE_DRY_RUN=true`, `LIVE_REAL_ORDER_ARMED=false`.
* After cleaning `live.verify.env`, Bithumb private API authentication now succeeds:

  * `/v1/accounts` returns HTTP 200.
  * `/v1/orders/chance` returns HTTP 200.
  * `/v1/accounts` returns KRW balance `395113.99879`, matching the mobile app.
* A stale `order_rule_snapshots` row caused a false canary failure:

  * `id=8700`
  * `source_mode=local_fallback`
  * `fallback_used=1`
  * `fallback_reason_code=AUTH_JWT_VERIFICATION`
  * Once deleted, `broker-diagnose` changed from `overall=FAIL` to `overall=WARN`, and `chance contract drift canary` passed with `change_detected=0`.
* Remaining blocker:

  * `BALANCE_SPLIT_MISMATCH`
  * `local=395113.99879`
  * `broker=1000000`
  * `delta=604886.00121`
* The `broker=1000000` value appears to come from `DryRunBalanceSource -> settings.START_CASH_KRW`, not from `/v1/accounts`.
* Do not “fix” this by adjusting the DB cash to `1,000,000`. Local cash already matches the real account balance.
* Current concern: the live dry-run system is mixing real exchange/account evidence, simulation balance, and local fallback snapshots into the same broker-truth layer.

Your job is to turn this into a robust, reproducible, operator-safe live dry-run / live promotion system. Prioritize design changes over symptom patches.

Work in the repo. Inspect at least these areas before patching:

* `src/bithumb_bot/broker/balance_source.py`
* `src/bithumb_bot/broker/bithumb.py`
* `src/bithumb_bot/runtime_readiness.py`
* `src/bithumb_bot/reporting.py`
* `src/bithumb_bot/config.py`
* `src/bithumb_bot/db_core.py`
* order-rule sync / snapshot logic
* broker diagnose / recovery-report / reconcile command paths
* relevant tests:

  * `tests/test_live_preflight.py`
  * `tests/test_order_rules_sync.py`
  * `tests/test_operator_commands.py`
  * `tests/test_bithumb_private_api.py`
  * any recovery/readiness tests

Core hypotheses to validate or falsify:

1. Live dry-run broker truth is currently mixed with simulation balance.

   * Validate whether `MODE=live + LIVE_DRY_RUN=true + /v1/accounts success` can still cause `DryRunBalanceSource` or `START_CASH_KRW` to enter recovery/resume/broker-local convergence as broker truth.
   * If confirmed, redesign so real account evidence and simulation balance are separate authorities.
   * In live dry-run:

     * `broker_cash_truth` must be `/v1/accounts` / `accounts_v1_rest_snapshot`.
     * `broker_position_truth` must be `/v1/accounts` / `accounts_v1_rest_snapshot`.
     * `dry_run_static` may be used only for execution simulation or no-submit simulation, never as broker truth for resume safety.

2. Fallback order-rule snapshots can currently become durable canary baselines.

   * Validate whether `order_rule_snapshots` rows with `fallback_used=1`, `source_mode=local_fallback`, or auth-failure reasons can be selected as canary baselines.
   * If confirmed, change baseline selection so `fallback_used=1` and auth-failed snapshots are excluded.
   * Prefer a clear trust policy over ad hoc filtering:

     * trusted baseline: exchange-verified or merged exchange snapshot with `fallback_used=0`
     * untrusted/quarantine: local fallback, auth-failed fallback, transport-failed fallback
   * Fallback snapshots may remain useful as emergency runtime fallback evidence, but must not become drift baselines.

3. Env/runtime provenance is not strong enough for live operations.

   * Validate whether service, CLI, healthcheck, and backup/timer paths can load different env sources or hide meaningful config drift.
   * Strengthen live command output with an env contract fingerprint:

     * env source key
     * env file path
     * env file exists/loaded
     * env file mtime/inode if feasible
     * API key length
     * API secret length
     * safe hash prefix of sensitive values, never raw secrets
     * git SHA / dirty status
   * Emit a clear event if sensitive credential length/fingerprint changes across live runs, without exposing secrets.

4. `broker-diagnose` currently allows misleading PASS output.

   * Validate whether `broker-diagnose` shows `balance query: cash_available=1,000,000 PASS` in live dry-run even when `/v1/accounts` returns `395113.99879`.
   * Redesign output so operators can distinguish:

     * `accounts_v1_cash`
     * `accounts_v1_asset_qty`
     * `dry_run_static_cash`
     * `balance_authority`
     * `simulation_balance_source`
   * PASS/WARN/FAIL should be authority-aware, not just “a balance object exists”.

5. Live experiment promotion is not fully reproducible yet.

   * Validate whether a live dry-run can be reconstructed from a single manifest.
   * If not, introduce or propose an `ExperimentManifest` / live run manifest that records:

     * git SHA and dirty status
     * env fingerprint
     * approved profile path/hash/status
     * order-rule snapshot id/signature/trust level
     * account snapshot hash or sanitized evidence summary
     * DB schema fingerprint
     * strategy params
     * execution engine / target-delta shadow flags
     * decision telemetry range
   * All recovery reports and decision telemetry should be able to reference this manifest id.

Implementation priorities:

1. Implement a Balance Authority Matrix.

   * Add explicit code-level policy for which source is authoritative for each use case:

     * broker account truth
     * broker position truth
     * resume/recovery safety gate
     * order sizing
     * simulation balance
     * paper portfolio balance
   * Live dry-run must use accounts-v1 evidence for broker truth when `/v1/accounts` succeeds.
   * Missing BTC row in live dry-run with KRW present must be treated as known-zero BTC only when policy allows `pass_no_position_allowed`.
   * Do not allow `START_CASH_KRW` to influence live recovery/resume safety.

2. Implement or harden Snapshot Trust Policy for order rules.

   * Ensure canary baseline selection excludes:

     * `fallback_used=1`
     * `source_mode=local_fallback`
     * `fallback_reason_code` indicating auth/transport failure
   * Keep fallback rows only as fallback evidence, not trusted exchange baselines.
   * Add structured trust metadata where appropriate; if a schema migration is too broad for this patch, implement safe selection rules and document a follow-up migration.

3. Redesign diagnostics and reports around evidence authority.

   * `broker-diagnose` must show actual account evidence separately from dry-run simulation balance.
   * `recovery-report` and readiness output must show:

     * selected broker truth source
     * selected simulation source
     * whether base row was missing and why that is allowed
     * whether the source is trusted, fallback, stale, or quarantined
   * The output should make it impossible to mistake `START_CASH_KRW=1000000` for real broker cash.

4. Add a live env lint / contract check if feasible in scope.

   * At minimum, add code/tests for detecting:

     * placeholder values such as `<verified-live_dry_run-profile>`
     * trailing whitespace on secret-bearing keys
     * deprecated ignored keys like `BUY_PRICE_NONE_MARKET_TO_PRICE_ALIAS_ENABLED`
     * paper-only keys in live env
     * risky live limits such as `MAX_DAILY_ORDER_COUNT=500`
     * missing approved profile for real live promotion
   * Do not print raw secrets.

Testing requirements:

Add focused regression tests. Do not rely on real Bithumb network calls in tests.

Required tests:

1. Live dry-run account evidence test:

   * Mode: `MODE=live`, `LIVE_DRY_RUN=true`, `LIVE_REAL_ORDER_ARMED=false`.
   * Mock `/v1/accounts` with KRW balance `395113.99879` and no BTC row.
   * Set or simulate `START_CASH_KRW=1000000`.
   * Expected:

     * broker cash truth is `395113.99879`
     * broker BTC quantity is known-zero under `pass_no_position_allowed`
     * no `BALANCE_SPLIT_MISMATCH` is produced when local cash equals `395113.99879`
     * dry-run static balance is visible only as simulation balance, not broker truth.

2. Recovery/resume blocker test:

   * Same account evidence as above.
   * Local DB cash equals accounts-v1 KRW.
   * Expected:

     * recovery/resume logic does not block on `PORTFOLIO_BROKER_CASH_MISMATCH`
     * if local DB cash differs from accounts-v1 KRW, blocker should use actual accounts-v1 values, not `START_CASH_KRW`.

3. Order-rule canary baseline test:

   * Insert or mock latest `order_rule_snapshots` row:

     * `source_mode=local_fallback`
     * `fallback_used=1`
     * `fallback_reason_code=AUTH_JWT_VERIFICATION`
   * Also provide an older valid merged/exchange row.
   * Expected:

     * canary baseline ignores the fallback row
     * canary compares against the trusted row
     * no false drift is reported when the current exchange snapshot matches the trusted row.

4. Broker diagnose output test:

   * In live dry-run, mocked accounts-v1 returns `395113.99879`.
   * `START_CASH_KRW=1000000`.
   * Expected output includes:

     * `accounts_v1_cash=395113.99879` or equivalent structured field
     * `dry_run_static_cash=1000000` or equivalent structured field
     * `balance_authority=accounts_v1_rest_snapshot`
   * Expected output must not present `cash_available=1000000` alone as broker balance truth.

5. Env/provenance test:

   * Verify live command contract emits safe env metadata and does not expose raw secrets.
   * Verify missing/placeholder approved profile is surfaced as not configured, not silently accepted.
   * Verify deprecated ignored env keys are reported clearly or linted.

Recovery and migration constraints:

* Do not delete production DB rows as part of normal startup.
* If you add cleanup logic, it must be explicit, operator-driven, backed up, and auditable.
* Fallback/auth-failed snapshots should be quarantined or excluded from baseline selection, not blindly removed.
* Preserve backward compatibility for existing DBs unless a migration is clearly justified.
* Any schema migration must be idempotent and covered by tests.
* Do not change live order submission behavior unless required by the authority separation; this task is primarily evidence, safety, diagnostics, and readiness correctness.
* Keep `EXECUTION_ENGINE=lot_native` as live behavior; target-delta shadow behavior may remain telemetry unless tests prove otherwise.

Observability requirements:

* Add structured log fields or report fields for:

  * `balance_authority`
  * `broker_truth_source`
  * `simulation_balance_source`
  * `accounts_v1_preflight_outcome`
  * `accounts_v1_row_count`
  * `accounts_v1_currencies`
  * `base_currency_missing_policy`
  * `order_rule_snapshot_id`
  * `order_rule_trust_level` or equivalent
  * `order_rule_fallback_used`
  * `order_rule_fallback_reason_code`
  * `canary_baseline_snapshot_id`
  * `canary_baseline_trusted`
  * `env_contract_fingerprint`
* Ensure `broker-diagnose`, `recovery-report`, and `reconcile` outputs are understandable to an operator without reading source code.

Out of scope unless necessary:

* Do not implement a full trading strategy rewrite.
* Do not enable real orders.
* Do not convert target-delta from shadow to live execution.
* Do not create or assume an approved strategy profile unless tests require a placeholder fixture.
* Do not patch by setting `START_CASH_KRW=395113.99879` in live env; that is a workaround and should not be the design.

Expected final validation commands:

Run the relevant test subset and report exact commands/results, for example:

```bash
uv run pytest -q tests/test_live_preflight.py tests/test_order_rules_sync.py tests/test_operator_commands.py
uv run pytest -q tests/test_bithumb_private_api.py
uv run pytest -q <new_or_modified_tests>
```

If the full suite is feasible, also run:

```bash
uv run pytest -q
```

Final report format:

1. Summary

   * What was changed.
   * Which hypotheses were supported, rejected, or left unresolved.

2. Root-cause analysis

   * Do not state unverified assumptions as facts.
   * Explain the authority-mixing issue and fallback-baseline issue separately.

3. Design changes

   * Balance Authority Matrix changes.
   * Snapshot Trust Policy changes.
   * Diagnostics/reporting changes.
   * Env/provenance changes.

4. Files changed

   * List every modified file and the purpose of the change.

5. Tests

   * List added/modified tests.
   * Include exact commands run and results.

6. Operational impact

   * Explain what should change in EC2 output after this patch.
   * Explicitly state that local DB cash should not be adjusted to `1,000,000`.
   * Explain how an operator should distinguish real account evidence from simulation balance.

7. Remaining follow-ups

   * Approved profile generation.
   * Live env lint hardening if not fully implemented.
   * Experiment manifest implementation if only partially implemented.
   * Any schema migration follow-up if trust metadata was implemented as selection filtering only.
