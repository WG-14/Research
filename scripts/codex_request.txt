You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Use the attached post-patch review as source context, but do not treat it as proven fact. Verify every claim against the repository before changing code. 

Your task is to perform a final hardening pass on the research execution-timing system. The latest review concluded that the major structural safety issues are largely closed: execution-side fills and portfolio-effective trades appear to be separated, regime evidence appears to use effective trades, execution event summaries appear to flow into reports/candidate profiles/promotion gates, and CLI output appears to expose execution event counts. Your job is not to reopen the large design. Your job is to verify that these safety invariants truly hold in `WG-14/bithumb-bot`, then apply only small, targeted patches for remaining observability, schema consistency, operator convenience, and low-risk performance issues.

Do not generalize this into advice. Inspect, patch, and test the actual repository.

Core invariant to preserve:

> A backtest must never confuse an execution-side fill with a portfolio-effective trade. Regime evidence, report summaries, promotion gates, promotion artifacts, and operator-facing CLI output must make it clear whether an event was attempted, filled at the execution layer, pending, skipped, failed, applied to the simulated portfolio, or closed as PnL evidence.

Current working judgment to verify:

* No major structural patch should be necessary if the current code already enforces the invariant above.
* Remaining work, if confirmed, should be small and additive:

  * expose execution event summary at promotion artifact top-level;
  * provide empty execution summaries on `not_enough_candles` / empty backtest paths;
  * show execution event summaries in `research-promote-candidate` CLI output;
  * optionally optimize top-of-book quote lookup with bisect/cursor if low risk.

Start by inspecting at least these files:

* `src/bithumb_bot/research/backtest_engine.py`
* `src/bithumb_bot/research/execution_timing.py`
* `src/bithumb_bot/research/dataset_snapshot.py`
* `src/bithumb_bot/research/execution_model/base.py`
* `src/bithumb_bot/research/execution_model/fixed_bps.py`
* `src/bithumb_bot/research/execution_model/stress.py`
* `src/bithumb_bot/research/validation_protocol.py`
* `src/bithumb_bot/research/promotion_gate.py`
* `src/bithumb_bot/research/cli.py`
* `src/bithumb_bot/research/run_summary.py`
* `src/bithumb_bot/market_regime/metrics.py`
* `tests/test_orderbook_top_research.py`
* `tests/test_research_backtest_reproducibility.py`
* `tests/test_research_promotion_gate.py`
* `tests/test_dataset_quality_report.py`
* Any current execution-timing, pending-fill, execution-event-summary, promotion, or CLI tests

Keep these as hypotheses until verified or falsified:

H1. The major safety invariant is now satisfied: execution-side fills and portfolio-applied trades are not conflated in trade records, regime coverage/performance, report summaries, candidate profiles, or promotion gates.

H2. `run_sma_backtest()` may still return `BacktestRun.execution_event_summary=None` on early-return paths such as `not_enough_candles`, causing schema inconsistency for direct callers and tests.

H3. Promotion artifacts may include execution event summaries only inside `candidate_profile`, not as top-level fields. This is hash-safe but less operator-friendly.

H4. `research-promote-candidate` CLI output may not show execution event summary fields even though `research-backtest` does. This weakens operator observability at the promotion step.

H5. Top-of-book quote lookup may still perform a linear scan for each signal. This is not a correctness issue, but dense quote data can make research runs scale poorly.

H6. Adding top-level promotion artifact fields must not weaken promotion gates, bypass candidate profile hash validation, or change safety semantics.

H7. Existing report/candidate/profile hashes should remain deterministic. Hashes may change only where new deterministic fields are intentionally added.

Implementation priorities:

P0. Verify safety invariants before patching.

Before making changes, confirm the following in code and tests:

* `record_type`, `is_execution_filled`, `is_portfolio_applied_trade`, `is_effective_trade`, `portfolio_application_status`, `portfolio_applied`, and related fields do not label pending executions as portfolio-applied trades.
* `aggregate_regime_coverage()` and `aggregate_regime_performance()` count only effective/portfolio-applied trades as regime trade evidence.
* Validation reports carry execution event summaries for train/validation/final holdout.
* Candidate profiles include execution event summaries in the profile hash.
* Promotion refuses incomplete execution timelines and pending execution after dataset end.
* `allow_legacy_lineage=True` does not bypass execution reality or execution event completeness checks.

If any of these fail, fix them as safety bugs before addressing P1/P2. Add regression tests for any failed invariant.

P1. Add top-level execution event summary fields to promotion artifacts.

Inspect `promote_candidate()` in `promotion_gate.py`.

If execution event summaries are only available inside `candidate_profile`, copy the most important summaries to the promotion artifact top level for operator readability. Add deterministic fields such as:

```text
execution_event_summary
train_execution_event_summary
validation_execution_event_summary
final_holdout_execution_event_summary
pending_execution_after_dataset_end_count
execution_event_timeline_incomplete
portfolio_applied_trade_count
execution_filled_count
closed_trade_count
```

Use existing candidate fields as the source. Do not recompute with different semantics inside promotion. Do not weaken or bypass existing candidate profile hash validation.

Add or update tests:

```text
test_promotion_artifact_exposes_execution_event_summary_top_level
test_promotion_artifact_execution_event_summary_matches_candidate_profile
test_promotion_artifact_hash_changes_when_execution_event_summary_changes
```

P1. Ensure early-return backtests include an empty execution event summary.

Inspect `run_sma_backtest()` early-return paths, especially `not_enough_candles`.

If early returns produce `BacktestRun.execution_event_summary=None`, add a deterministic empty summary helper, for example:

```python
empty_execution_event_summary() -> dict[str, object]
```

It should include all normal summary keys with zero counts and `execution_event_timeline_incomplete=False`, unless the code has a better explicit reason to mark the timeline incomplete.

Expected keys include at least:

```text
execution_attempt_count
execution_filled_count
filled_execution_count
portfolio_applied_trade_count
pending_execution_count
pending_execution_at_end_count
pending_execution_after_dataset_end_count
skipped_execution_count
failed_execution_count
closed_trade_count
execution_event_timeline_incomplete
```

Add tests:

```text
test_not_enough_candles_backtest_has_empty_execution_event_summary
test_empty_execution_event_summary_schema_matches_non_empty_summary
```

P1. Add execution event summary output to `research-promote-candidate` CLI.

Inspect `cmd_research_promote_candidate()` in `research/cli.py`.

If promotion output does not show execution event summary fields, add concise output lines using the artifact fields from P1. Include:

```text
execution_attempt_count
execution_filled_count
portfolio_applied_trade_count
pending_execution_count
pending_execution_after_dataset_end_count
skipped_execution_count
failed_execution_count
closed_trade_count
execution_event_timeline_incomplete
```

Keep output stable and deterministic. Do not print huge nested JSON blobs unless necessary.

Add tests if CLI tests exist. If not, add a focused test only if the repository already has a pattern for CLI output tests. Otherwise document the absence of a CLI test pattern in the final report.

P2. Optional low-risk quote lookup optimization.

Inspect `first_quote_after_or_equal()` and `DatasetSnapshot.sorted_execution_top_of_book_quotes()`.

If the lookup still linearly scans all sorted quotes for every signal and a low-risk improvement is straightforward, implement a deterministic bisect or cursor-based lookup:

* keep timestamp/source deterministic tie-breaks;
* do not change first-after-or-equal semantics;
* avoid mutable global cache;
* preserve dataset fingerprint determinism;
* avoid over-engineering.

Possible design:

```python
sorted_quotes = dataset.sorted_execution_top_of_book_quotes()
quote_keys = tuple((quote.ts, quote.source) for quote in sorted_quotes)
idx = bisect_left(...)
```

If the refactor would be invasive, defer it and explain why.

Add a test if optimized:

```text
test_quote_lookup_preserves_first_after_or_equal_tie_break
test_quote_lookup_returns_same_result_as_linear_scan_for_unsorted_input
```

Scope rules:

* Do not revert the event-time execution architecture.
* Do not change live trading behavior.
* Do not reintroduce candle-close or same-candle execution as promotion-grade evidence.
* Do not treat execution-side fills and portfolio-applied trades as the same concept.
* Do not weaken promotion refusal for execution reality, incomplete execution timelines, or pending execution after dataset end.
* Do not remove legacy compatibility fields unless all downstream use is intentionally migrated and tested.
* Prefer additive fields over breaking JSON consumers.
* Preserve deterministic reproducibility.
* Do not use live network calls in tests.
* Do not implement full-depth orderbook replay. Keep `top_of_book_is_full_depth=false` visible where applicable.
* If a hypothesis is already falsified by current code, do not patch it merely to satisfy this prompt. Report the verification evidence.

Required tests to run:

At minimum run:

```bash
pytest -q tests/test_research_backtest_reproducibility.py tests/test_orderbook_top_research.py tests/test_research_promotion_gate.py tests/test_dataset_quality_report.py
```

Also run every new or modified test.

If feasible, run:

```bash
pytest -q
```

Do not claim the full suite passed unless it was actually run.

Expected final report format:

Return a concise but complete implementation report with exactly these sections:

1. `Hypotheses checked`

   * For H1 through H7, state `verified`, `falsified`, or `partially verified`.
   * Include concrete files/functions/tests used as evidence.

2. `Design changes made`

   * Summarize any top-level promotion artifact changes.
   * Summarize any empty execution summary schema changes.
   * Summarize any promotion CLI output changes.
   * Summarize quote lookup optimization if implemented.
   * State explicitly if no safety patch was needed because existing code already satisfied the invariant.

3. `Behavior before vs after`

   * Explain how operators see execution event summaries before vs after.
   * Explain early-return backtest summary behavior before vs after.
   * Explain whether promotion artifacts now expose execution event summaries directly.
   * Explain whether quote lookup behavior changed or was deferred.

4. `Tests added/updated`

   * List each test name and the invariant it locks.
   * Include any existing tests updated.

5. `Promotion and safety impact`

   * State whether promotion gate semantics changed.
   * State whether execution event summaries are now visible at promotion artifact top-level.
   * State whether pending/incomplete execution timelines still block promotion.
   * State whether `allow_legacy_lineage=True` remains unable to bypass execution reality/event completeness checks.

6. `Observability and reproducibility`

   * List new or changed metadata/report/artifact/CLI fields.
   * State whether candidate profile hash, promotion artifact hash, report content hash, or manifest hash behavior changed.
   * State how deterministic behavior was preserved.

7. `Limitations and follow-up`

   * Mention no full-depth or intra-candle-path replay unless implemented.
   * Mention any remaining operator convenience gaps.
   * Mention quote lookup performance if optimization was deferred.

8. `Commands run`

   * Include exact commands and pass/fail results.
   * If any command was not run, say so and why.

Do not submit a final answer that only says tests pass. The final report must prove that the repository now has no remaining required structural patch, and that any changes made improve observability or schema consistency without weakening the execution-time safety contract.
