You are Codex working directly in the `WG-14/bithumb-bot` repository.

Task: apply a narrow but important follow-up hardening patch. The previous patch mostly closed the major reproducibility gaps: `research-reproduce` now recomputes report hashes, lineage-less promotion is no longer silently allowed by default, and decision-equivalence is tied into profile transition evidence. However, one remaining evidence-chain gap likely still exists: `research-promote-candidate` itself may bind source report hashes from embedded `content_hash` fields without recomputing those hashes from report body content.

Do not treat this as generic cleanup. The goal is to make promotion fail closed at artifact creation time, not only later during `research-reproduce`.

Core hypothesis:
`promote_candidate()` may still trust `backtest_report.json["content_hash"]` and `walk_forward_report.json["content_hash"]` instead of recomputing each report’s canonical content hash before creating a promotion artifact.

Verify this hypothesis from code before changing anything.

Inspect these files first:

* `src/bithumb_bot/research/promotion_gate.py`
* `src/bithumb_bot/research/lineage.py`
* `src/bithumb_bot/research/report_writer.py`
* `src/bithumb_bot/research/cli.py`
* `src/bithumb_bot/app.py`
* existing tests for promotion gate, lineage, research reproduce, and CLI dispatch

Expected current risk:
`research-reproduce` may now catch tampered reports, but `research-promote-candidate` may still create a promotion artifact from a tampered source report if the stale embedded `content_hash` field remains unchanged. That means promotion and reproduction do not yet share the same trust model.

Required design change:
Promotion must verify source report truth before binding report hashes into the promotion artifact.

Implement a promotion-gate helper similar to:

```python
def _verify_report_content_hash(report: dict[str, Any], *, label: str) -> str:
    expected = str(report.get("content_hash") or "").strip()
    if not expected.startswith("sha256:"):
        raise PromotionGateError(f"promotion refused: {label}_content_hash_missing")
    actual = sha256_prefixed(
        content_hash_payload({k: v for k, v in report.items() if k != "content_hash"})
    )
    if actual != expected:
        raise PromotionGateError(f"promotion refused: {label}_hash_mismatch")
    return actual
```

If the repository already has a report-writer-specific canonical hash helper, use that instead. Do not introduce a second incompatible hash convention.

Wire this into `promote_candidate()`:

* Immediately after loading `backtest_report.json`, recompute and verify its canonical report hash.
* Use the verified hash, not `report.get("content_hash")`, when setting:

  * `backtest_report_hash`
  * promotion lineage `backtest_report_hash`
* If walk-forward is required, immediately after loading `walk_forward_report.json`, recompute and verify its canonical report hash.
* Use the verified walk-forward hash, not `walk_report.get("content_hash")`, when setting:

  * `walk_forward_report_hash`
  * promotion lineage `walk_forward_report_hash`
* Preserve existing candidate/profile/lineage/walk-forward consistency checks.
* Do not weaken `allow_legacy_lineage` behavior.
* Do not make hash mismatch recoverable or warning-only.

Required failure behavior:

* Backtest report body tampered while old embedded `content_hash` remains → promotion fails.
* Walk-forward report body tampered while old embedded `content_hash` remains → promotion fails.
* Missing backtest report `content_hash` → promotion fails with a source-specific missing reason.
* Missing walk-forward report `content_hash` when walk-forward is required → promotion fails with a source-specific missing reason.
* Valid report hashes → promotion still passes.
* `research-reproduce` behavior must remain unchanged or stricter.

Required tests:

Add or update deterministic tests for:

* valid lineage-backed promotion still passes
* backtest report body tampered but stale embedded `content_hash` left unchanged → `promote_candidate()` fails with `backtest_report_hash_mismatch`
* backtest report missing `content_hash` → fails with `backtest_report_content_hash_missing`
* walk-forward required, walk-forward report body tampered but stale embedded `content_hash` left unchanged → fails with `walk_forward_report_hash_mismatch`
* walk-forward required, walk-forward report missing `content_hash` → fails with `walk_forward_report_content_hash_missing`
* promotion artifact uses verified recomputed hashes in both top-level fields and lineage fields
* `research-reproduce` still passes for a valid promotion artifact produced by `promote_candidate()`

Also confirm parser-level CLI wiring:

* Verify that top-level argparse dispatch in `app.py` exposes `--allow-legacy-lineage` for `research-promote-candidate`.
* If missing, add it.
* Add or update a CLI-level test if the repo already has CLI dispatch tests.
* If the flag is already correctly wired, do not change it unnecessarily; mention it in the final report.

Observability and recovery requirements:

* Failure messages must identify the exact source artifact:

  * `backtest_report_hash_mismatch`
  * `backtest_report_content_hash_missing`
  * `walk_forward_report_hash_mismatch`
  * `walk_forward_report_content_hash_missing`
* Do not emit vague errors like `invalid_report`.
* Do not log secrets or raw private environment values.
* Operator recovery should be obvious from the reason:

  * regenerate the backtest report
  * regenerate the walk-forward report
  * rerun promotion after source evidence is restored

Documentation:

Update the relevant docs/runbook if they currently imply source evidence is hash-verified during promotion. Make the behavior explicit:

```text
research-promote-candidate recomputes canonical backtest/walk-forward report hashes before binding them into the promotion artifact. It does not trust embedded report content_hash fields.
```

Also clarify that:

```text
research-reproduce repeats the same source artifact truth check later.
```

Scope control:

* Do not rewrite the trading strategy.
* Do not redesign the entire lineage system.
* Do not change decision-equivalence policy unless a test must be adjusted because this patch affects fixtures.
* Do not weaken approved-profile, evidence-chain, path-policy, or runtime verification.
* Do not silently accept malformed reports.
* Do not fabricate orderbook/depth/intra-candle data.
* Keep this patch focused on promotion-time source report hash verification and CLI flag confirmation.

Run tests:

Run targeted tests first, then full pytest.

At minimum:

```bash
pytest tests -q
```

If narrower tests exist for promotion, lineage, reproduce, approved profile, or CLI dispatch, run those explicitly before the full suite.

Final report format:

```text
Summary
- What changed and why.

Hypothesis evaluated
- Hypothesis: confirmed / partially confirmed / rejected.
  Evidence:
  Changes:

Design changes
- Modified modules:
- New/changed helpers:
- New/changed validation rules:
- CLI flag status:

Tests
- New tests:
- Updated tests:
- Negative cases:
- Positive cases:
- Commands run:
- Result:

Observability / recovery
- New failure reasons:
- Operator recovery path:

Docs
- Files updated:
- Behavior clarified:

Known limitations / follow-up
- Anything intentionally not solved in this patch.
```

The central priority is this: promotion and reproduction must share the same evidence trust model. `research-promote-candidate` must not merely copy embedded source report hashes into a promotion artifact; it must recompute and verify source report truth before creating that artifact.

