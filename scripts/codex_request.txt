You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

The latest patches appear to have closed the urgent P0 evidence-chain gaps and implemented the first top-of-book research data layer:

* `orderbook_top_snapshots` schema exists.
* top-of-book public quote validation/storage exists.
* `sync-orderbook-top` exists.
* `dataset.top_of_book` manifest parsing exists and fails closed on unsupported fields.
* research dataset loading can join nearest top-of-book snapshots by candle timestamp and tolerance.
* dataset quality reports include quote coverage when top-of-book is configured.
* `ExecutionRequest` receives `best_bid`, `best_ask`, and `spread_bps` when joined quote data exists.
* docs now correctly say top-of-book is not full depth and does not solve intra-candle path.
* pytest reportedly passes.

Your task is **not** to redo the top-of-book implementation. Your task is to perform the next small hardening pass:

1. Remove or isolate test-only strategy names from the production/operator-facing research strategy registry.
2. Improve operator visibility for optional top-of-book quote coverage warnings.
3. Keep quote-aware execution policy out of scope unless a minimal, fully tested hook already exists.

Do not weaken the core problem statement:

This repository is becoming a reproducible, evidence-bound automatic trading experiment system. Candle-only research remains valid only for candle-appropriate strategies. Top-of-book metadata improves execution feasibility evidence, but it is still not full orderbook depth, queue position, tick data, market impact, or intra-candle path. Operator-facing research reports must make missing optional quote coverage visible without silently treating it as full evidence.

Do not turn this into generic cleanup. Make concrete, testable repository changes.

Inspect first:

* `src/bithumb_bot/research/strategy_registry.py`
* `src/bithumb_bot/research/validation_protocol.py`
* `src/bithumb_bot/research/dataset_snapshot.py`
* `src/bithumb_bot/research/backtest_engine.py`
* `src/bithumb_bot/research/cli.py`
* `src/bithumb_bot/research/experiment_manifest.py`
* `src/bithumb_bot/research/execution_model/base.py`
* `src/bithumb_bot/app.py`
* `src/bithumb_bot/orderbook_top_store.py`
* `tests/test_orderbook_top_store.py`
* existing tests covering research backtest, dataset quality, CLI summaries, strategy registry, promotion, and reproducibility
* `docs/research-validation.md`

Keep these as hypotheses until verified in code.

Hypothesis 1:
A test-only strategy name such as `top_of_book_required_test` is exposed through the production research strategy registry and can be used in a real operator manifest.

Verification:
Inspect `strategy_registry.py` and all tests that depend on this name. Determine whether it is intended to be a production-supported strategy or only a test hook.

Required behavior:
Production/operator-facing strategy names must remain clean. Test-only strategy names must not look like real supported strategies.

Acceptable fixes:

Option A — preferred:
Rename the test-only strategy to a clearly private test hook, for example:

```python
"__test_top_of_book_required__"
```

and make the naming convention explicit in tests.

Option B:
Remove the test-only strategy from `resolve_research_strategy()` and keep only a test helper for data requirement validation.

Option C:
Gate test-only strategy resolution behind an explicit internal/test flag, if such a pattern already exists in the repo.

Constraints:

* `sma_with_filter` must remain supported with `required_data=("candles",)` and `optional_data=("top_of_book",)`.
* Unknown real strategy names must continue to fail with an operator-readable unsupported strategy error.
* Do not add fake microstructure strategy support.
* Do not make `top_of_book_required_test` appear in docs as a production strategy.
* If any test-only strategy remains, its name must clearly communicate that it is not an operator strategy.

Reason code requirement:
If a strategy requires top-of-book and the manifest lacks `dataset.top_of_book`, fail closed with:

```text
research_data_requirement_top_of_book_missing
```

or preserve this exact existing reason if already implemented.

Hypothesis 2:
Optional top-of-book missing coverage currently appears inside split-level dataset quality reports as `top_of_book_gate_status="WARN"`, but may not be visible in candidate-level warnings or the CLI summary. Operators should not need to open raw JSON to notice optional quote coverage problems.

Verification:
Inspect report payload construction and research CLI summary formatting. Determine whether optional quote WARN is propagated to:

* top-level report summary
* candidate `warnings`
* CLI output / operator next action

Required behavior:
When `dataset.top_of_book` is configured with `required=false` and quote coverage is incomplete, research should still run, but the warning must be visible.

Add candidate warning, for example:

```text
top_of_book_optional_coverage_warning
```

Include enough context in report payload or CLI output to show:

* `top_of_book_gate_status`
* `top_of_book_coverage_pct`
* `top_of_book_missing_count`
* affected split names
* next action

Suggested next action text:

```text
collect orderbook top snapshots with sync-orderbook-top, rerun research-backtest, and verify top_of_book_coverage_pct
```

Required behavior for required top-of-book:
If `dataset.top_of_book.required=true` or `missing_policy=fail`, missing or insufficient quote coverage must remain fail-closed and must propagate deterministic reasons into candidate gates. Do not convert required failures into warnings.

Hypothesis 3:
Top-of-book execution metadata is now carried through research trades, but fill pricing still uses candle close plus fixed/stress bps. This is acceptable for this patch, but docs and reports must not imply quote-aware execution pricing.

Verification:
Inspect execution metadata and docs. Confirm that quote fields are metadata only unless an explicit execution model changes reference pricing.

Required behavior:

* Keep `reference_price` as candle close unless there is already an explicit quote-aware execution model.
* Continue recording `best_bid`, `best_ask`, and `spread_bps` when joined quotes exist.
* Continue recording limitations when quotes are absent.
* Do not implement spread gate or quote-aware execution pricing in this patch unless it is already partially implemented and can be finished safely with tests.
* Do not claim full depth, market impact, queue position, or intra-candle path support.

Hypothesis 4:
CLI coverage for `sync-orderbook-top` may be incomplete even if the command function exists.

Verification:
Inspect command registration in `app.py` and tests. Confirm whether `sync-orderbook-top` is registered, callable, and tested with mocked public API output.

If missing or weak, add tests for:

* command registration
* mocked successful quote fetch
* DB row written
* output includes pair, bid, ask, spread_bps, source, row_count
* output includes next action
* invalid quote fails before insert
* no repo-local artifact writes

Do not call the real Bithumb API in tests.

Hypothesis 5:
The top-of-book feature is documented, but operator-facing failure modes may still be too buried.

Verification:
Inspect `docs/research-validation.md` and any runbook or operator reporting docs.

Required docs updates:
Only update docs to match implemented behavior.

Ensure docs clearly state:

* `sma_with_filter` supports candle-only research.
* `dataset.top_of_book` is optional unless required by strategy or manifest.
* optional top-of-book missing coverage produces visible warning, not silent success.
* required top-of-book missing coverage fails closed.
* top-of-book is not full depth.
* top-of-book does not solve intra-candle path.
* `sync-orderbook-top` collects only current public best bid/ask snapshots.
* research quote joins use nearest snapshot within tolerance.
* operators should rerun research after collecting quote snapshots.
* quote-aware execution pricing, spread gate, full depth, ticks, and path replay remain future work unless implemented.

Scope limits:

This is a hardening and observability patch, not a new execution model patch.

Out of scope unless already trivial and fully tested:

* spread gate
* quote-aware fill pricing
* full orderbook depth
* trade ticks
* queue position
* market impact
* intra-candle path reconstruction
* latency-aware quote replay
* live order policy changes
* live arming behavior changes

Testing requirements:

Add or update tests. Do not rely on manual review.

Minimum tests:

Strategy registry / data requirements:

1. `sma_with_filter` remains a supported production research strategy.
2. `sma_with_filter` has candles required and top-of-book optional.
3. Unknown strategy names fail clearly.
4. Any test-only top-of-book-required strategy name is clearly private or test-scoped.
5. A top-of-book-required test hook fails closed when `dataset.top_of_book` is absent.
6. The fail reason includes `research_data_requirement_top_of_book_missing`.

Optional quote warning visibility:

7. Optional top-of-book configured but missing quotes does not fail candidate solely because quotes are missing.
8. The report includes `top_of_book_gate_status="WARN"`.
9. Candidate warnings include `top_of_book_optional_coverage_warning` or equivalent deterministic warning.
10. CLI summary prints top-of-book coverage status, missing count, and next action.
11. The warning is deterministic and included in content-hashed report payload if appropriate.

Required quote fail-closed:

12. Required top-of-book missing quotes fails dataset quality and candidate gate.
13. Gate reasons include top-of-book missing or coverage-below-threshold reason.
14. Promotion remains impossible for failed required quote coverage.

Execution metadata:

15. When joined quotes exist, research trade execution metadata includes `best_bid`, `best_ask`, and `spread_bps`.
16. When quotes are absent, execution metadata does not fabricate values.
17. Existing candle-only research still works and records the correct limitations.

CLI / ingestion:

18. `sync-orderbook-top` is registered and callable.
19. Mocked quote fetch writes one DB row.
20. CLI output includes pair, bid, ask, spread_bps, source, row_count, and next action.
21. Tests do not hit the real network.

Regression:

22. Existing dataset quality, execution calibration, promotion, and research reproducibility tests still pass.

Run at least:

```bash
uv run pytest -q
```

If full pytest is too slow, run targeted subsets first, then full pytest if feasible. In the final report, state exactly what was run.

Operational and recovery constraints:

* Do not weaken live safety gates.
* Do not change live arming behavior.
* Do not change real order submission behavior.
* Do not write runtime artifacts into the repository.
* Use existing runtime DB/path policy.
* Preserve deterministic hashing.
* Do not silently ignore unsupported manifest fields.
* Do not fabricate quote/depth/path data.
* Keep new warnings and fail reasons deterministic, reason-coded, and searchable.
* Avoid heavy dependencies.
* Keep top-of-book optional for current SMA research unless explicitly required.

Observability requirements:

Research reports and CLI summaries should make the following visible when relevant:

* top-of-book requested
* top-of-book required
* join tolerance
* joined quote count
* missing quote count
* quote coverage percentage
* quote gate status
* affected split names
* whether this is a warning or a fail-closed condition
* next action for operators
* explicit limitation that top-of-book is not full depth
* explicit limitation that intra-candle path is still unavailable

Final report format:

Return a concise engineering report with these sections:

1. Summary

   * What was verified.
   * What was changed.
   * What remains out of scope.

2. Hypothesis Review
   For each hypothesis 1–5:

   * status: verified / refuted / partially verified / not fully investigated
   * code evidence
   * resulting action

3. Implemented Changes

   * Files changed
   * New or changed strategy names, warning codes, report fields, CLI output, tests
   * Backward compatibility behavior

4. Validation and Tests

   * Exact commands run
   * Test results
   * Any tests not run and why

5. Operational Impact

   * How operators see optional quote warnings
   * How required quote failures surface
   * How candle-only research behavior changes or does not change
   * Whether promotion/live readiness behavior changed

6. Remaining Risks / Follow-up

   * quote-aware execution pricing
   * spread gate
   * full depth
   * trade ticks
   * queue position
   * market impact
   * intra-candle path
   * long-range quote backfill

7. Suggested Next PRs

   * P1/P2/P3 priority list for anything not completed

Do not produce a generic architecture essay. Make concrete repository changes where feasible, run tests, and report with exact file/function references.
