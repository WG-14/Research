You are Codex working directly in the GitHub repository `WG-14/bithumb-bot`.

Use the attached latest review as source context, but verify every claim against the current repository before modifying code. Do not treat the review as automatically true. 

Your task is to review the current paper stress execution patch and apply the next necessary hardening patch. The current implementation appears to have resolved the major lifecycle gaps: paper stress execution now has an unresolved-order gate, fail-closed model/rate validation, SELL stress coverage, and latency tests. The remaining priority is a smaller but important lifecycle semantics bug: `PAPER_EXECUTION_PARTIAL_FILL_FRACTION` currently appears to allow boundary values `0.0` and `1.0`, which can make a stress result say `fill_status="partial"` while the actual filled quantity is zero or fully filled.

Do not turn this into general advice. Work in this repo, verify the code, patch the edge case, and add tests that would fail before the patch.

## Primary files to inspect

Inspect these files first:

* `src/bithumb_bot/broker/paper.py`
* `src/bithumb_bot/broker/paper_execution.py`
* `src/bithumb_bot/research/execution_model/stress.py`
* `tests/test_paper_stress_execution.py`
* `tests/test_paper_orderbook.py`
* `docs/paper-execution-lifecycle.md`
* `.env.example`

Also search before adding new code:

```text
PAPER_EXECUTION_PARTIAL_FILL_FRACTION
_validate_paper_execution_config
_validate_unit_interval_config
StressPaperExecutionAdapter
StressExecutionModel
fill_status == "partial"
remaining_qty
PARTIAL
```

## Working hypotheses to verify, not assume

Keep these hypotheses explicit. Confirm, refine, or refute each one in the final report.

### H1: The major paper lifecycle patch is now mostly in place

Verify whether paper execution currently:

* imports and calls `evaluate_unresolved_order_gate`
* blocks a new order when a previous `PARTIAL` order remains open
* validates `PAPER_EXECUTION_MODEL`
* validates latency/rate settings
* has BUY and SELL stress tests
* asserts latency in fill/trade timestamps and evidence

Refutation:

* If any of these are missing or broken, fix them before the smaller boundary patch.

### H2: `PAPER_EXECUTION_PARTIAL_FILL_FRACTION=0.0` can produce contradictory evidence/status semantics

Verify whether the current validation allows `0.0`.

Risk:

* Research stress model can emit `fill_status="partial"` with `filled_qty=0`.
* Paper execution may record partial-style evidence, then later classify the order as `FAILED` because `filled_qty <= 0`.
* This creates a mismatch between stress evidence and final order state.

Refutation:

* If the current code already rejects `0.0`, document the exact validation and ensure there is a regression test.

### H3: `PAPER_EXECUTION_PARTIAL_FILL_FRACTION=1.0` can leave a fully filled order as `PARTIAL`

Verify whether the current validation allows `1.0`.

Risk:

* Research stress model can emit `fill_status="partial"` with `filled_qty == requested_qty`.
* Paper execution may set final status to `PARTIAL` based only on `fill_status`, even though `qty_filled == qty_req`.
* This can leave a fully filled order open in OMS/dedup, causing unresolved-order gate to block future paper execution forever or until manual cleanup.

Refutation:

* If current code normalizes this to `FILLED`, document the exact path and add a test proving it.

### H4: Boundary fraction validation is the correct first fix

The priority fix is to fail closed on invalid partial-fill semantics rather than trying to reinterpret boundary partial results after execution.

Expected rule:

* `PAPER_EXECUTION_PARTIAL_FILL_FRACTION` must be strictly greater than `0.0` and strictly less than `1.0` for paper stress semantics.

Refinement:

* If repository conventions strongly prefer conditional validation, it is acceptable to require `0 < fraction < 1` only when `PAPER_EXECUTION_MODEL=stress`. Do not silently allow boundary values in active stress mode.

## Required patch

Patch paper stress configuration validation so `PAPER_EXECUTION_PARTIAL_FILL_FRACTION` cannot be `0.0` or `1.0` in stress mode.

Preferred behavior:

```text
PAPER_EXECUTION_MODEL=stress
PAPER_EXECUTION_PARTIAL_FILL_FRACTION=0.0  -> ValueError
PAPER_EXECUTION_MODEL=stress
PAPER_EXECUTION_PARTIAL_FILL_FRACTION=1.0  -> ValueError
```

Error message should mention `PAPER_EXECUTION_PARTIAL_FILL_FRACTION` and clearly state that paper partial-fill semantics require a value strictly between 0 and 1.

A straightforward implementation is acceptable:

```python
partial_fraction = _validate_unit_interval_config(
    "PAPER_EXECUTION_PARTIAL_FILL_FRACTION",
    getattr(settings, "PAPER_EXECUTION_PARTIAL_FILL_FRACTION", 0.5),
)
if partial_fraction <= 0.0 or partial_fraction >= 1.0:
    raise ValueError(
        "PAPER_EXECUTION_PARTIAL_FILL_FRACTION must be > 0 and < 1 "
        f"for paper partial-fill semantics, got {partial_fraction!r}"
    )
```

Place this where it best fits the current repository style. If `_validate_paper_execution_config()` already exists, prefer updating it rather than adding a second validation path.

## Required tests

Add focused tests that would fail before this patch.

Suggested test:

```python
@pytest.mark.parametrize("invalid_fraction", [0.0, 1.0])
def test_invalid_paper_partial_fill_fraction_boundary_fails_closed(invalid_fraction):
    old_model = _set("PAPER_EXECUTION_MODEL", "stress")
    old_fraction = _set("PAPER_EXECUTION_PARTIAL_FILL_FRACTION", invalid_fraction)
    try:
        with pytest.raises(ValueError, match="PAPER_EXECUTION_PARTIAL_FILL_FRACTION"):
            paper._validate_paper_execution_config()
    finally:
        _set("PAPER_EXECUTION_PARTIAL_FILL_FRACTION", old_fraction)
        _set("PAPER_EXECUTION_MODEL", old_model)
```

If direct helper testing is not the repo style, call `paper.paper_execute()` with minimal setup, but avoid making the boundary validation test depend on unrelated orderbook/DB behavior if possible.

Also ensure existing valid stress configuration still passes:

```text
PAPER_EXECUTION_PARTIAL_FILL_FRACTION=0.5
```

Do not weaken existing tests for:

* unresolved-order gate
* duplicate intent
* SELL partial/failure
* latency timestamp shift
* deterministic replay
* failure without fills/trades

## Optional improvement, lower priority

Currently verify whether `paper_execute()` fetches orderbook before checking unresolved order gate. If open order exists, fetching orderbook first can hide the unresolved-gate skip behind an orderbook failure. This is not the primary fix, but if the refactor is small and safe, prefer this order:

```text
validate config
resolve market
open DB
init portfolio
evaluate unresolved order gate
then fetch orderbook / quote
```

If this reorder touches many compatibility paths or legacy tests, do not over-scope. Document it as remaining work.

## Recovery and observability requirements

This patch must preserve lifecycle safety:

* `PARTIAL` must remain open only when `0 < qty_filled < qty_req`.
* A fully filled order must not be left as `PARTIAL`.
* A zero-filled “partial” must not be recorded as partial evidence and then failed later.
* Dedup semantics must not be weakened.
* Unresolved-order gate behavior must remain unchanged.
* No new DB schema is needed for this patch.

If you update docs, keep them precise:

* Stress partial-fill fraction must be strictly between 0 and 1.
* Boundary values are rejected because they break paper lifecycle semantics.
* Async delayed fill scheduling and live-dry-run chaos remain future work unless actually implemented and tested.

## Scope boundaries

Do not:

* implement live-dry-run chaos broker in this patch
* redesign `StressExecutionModel`
* change real live order submission
* relax paper stress validation
* silently clamp `PAPER_EXECUTION_PARTIAL_FILL_FRACTION`
* reinterpret `partial_fill_fraction=1.0` as filled unless you also prove and document that semantics across evidence, status, dedup, and tests; failing closed is preferred

## Tests to run

Run focused tests first:

```bash
pytest tests/test_paper_stress_execution.py -q
pytest tests/test_paper_orderbook.py -q
```

Then run adjacent regression tests if feasible:

```bash
pytest tests/test_ledger_atomicity.py -q
pytest tests/test_recovery_restart_regression.py -q
pytest tests/test_operator_commands.py -q
pytest tests/test_research_backtest_reproducibility.py -q
```

If a test fails for reasons unrelated to this patch, document the exact command, failure, and why it is unrelated. Do not hide failures.

## Final report format

When finished, report in exactly this structure:

```text
Summary
- What changed.
- Whether this was a code patch, test patch, or documentation patch.
- Whether default immediate paper behavior remains preserved.

Hypothesis Review
- H1: confirmed/refined/refuted, with repo evidence.
- H2: confirmed/refined/refuted, with repo evidence.
- H3: confirmed/refined/refuted, with repo evidence.
- H4: confirmed/refined/refuted, with repo evidence.

Implementation Details
- Files changed.
- Where partial-fill fraction boundary validation was added.
- Whether validation applies only in stress mode or globally, and why.
- Any docs updated.

Behavioral Guarantees
- What happens for `PAPER_EXECUTION_PARTIAL_FILL_FRACTION=0.0`.
- What happens for `PAPER_EXECUTION_PARTIAL_FILL_FRACTION=1.0`.
- What happens for normal `0 < fraction < 1`.
- Why this prevents `fill_status="partial"` from contradicting actual fill quantity/status.
- Dedup/recovery implications.

Tests
- New tests added.
- Existing tests updated.
- Exact pytest commands run.
- Pass/fail results.

Remaining Gaps
- Whether orderbook-before-unresolved-gate ordering remains as-is.
- Whether async delayed fill scheduling remains future work.
- Whether live-dry-run chaos remains future work.

Do Not Overclaim
- Explicitly state anything not implemented.
```

The priority is to preserve paper stress lifecycle truthfulness: a `PARTIAL` status must mean a real partial fill, not zero fill and not full fill.

